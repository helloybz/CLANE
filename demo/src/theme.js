import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#fff",
            dark: "#fff"
        },
        background: {
            dark: "#0D1928"
        },
        text: {
            dark: "#fff"
        },
        divider: {
            dark: "#1E3A5C"
        }
    },
});